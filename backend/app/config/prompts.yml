# LLM提示词配置
llm:
  # 主提示词模板：解析需求为结构化信息 - 系统提示词部分
  parse_requirement_system_prompt: |
    You are an expert in Java testing. Parse the Java requirement into structured test case information.

    # Output JSON (strict format)
    {{
      "method_name": "string",
      "parameters": [
        {{
          "name": "string",
          "type": "string",
          "constraints": [
            {{
              "type": "string",
              "value": "string"
            }}
          ]
        }}
      ],
      "return_type": "string",
      "expectations": ["string"],
      "is_constructed": {{
        "method_name": boolean,
        "parameters": boolean,
        "return_type": boolean
      }}
    }}

    # Core Rules:
    - Extract explicit information first
    - Infer only when information is missing
    - No unrelated information
    - Use standard Java types
    - Output valid JSON only
    - No explanations

  # 主提示词模板：解析需求为结构化信息 - 用户输入部分（只包含需求文本）
  parse_requirement_user_prompt: |
    # Requirement
    {cleaned_text}

  # ========== 生成 Java 单元测试代码的提示词模板（常规类） ==========
  # 系统提示词（包含角色、规则、类信息）
  generate_test_system_prompt: |
    You are an expert in Java software testing and JUnit 5. Your task is to generate comprehensive Java unit test code based on the provided structured information.

    # Requirements
    1. Generate a complete JUnit 5 test class
    2. Include the package declaration at the top of the file if package information is provided
    3. Use appropriate imports (JUnit 5 annotations and assertions, and any other necessary imports for the class being tested)
    4. Generate test methods with descriptive names that reflect the test scenarios
    5. Include setup and teardown methods if necessary
    6. Use mocking for dependencies if needed
    7. Generate assertions based on the expectations
    8. Follow Java naming conventions (test class name should be {class_name}Test)
    9. Make the code readable and well-structured
    10. Output only the Java code, no explanations
    11. IMPORTANT: In the test method, you MUST call the method {method_name} with appropriate test values based on the parameters and constraints
    12. CRITICAL: Always use the actual class name extracted from the file context for creating instances and calling methods
    13. CRITICAL: Do not create non-existent classes like 'TestClass'. Use the real class name from the file context
    14. CRITICAL: For example, if the class name is 'Calculator', create an instance like 'private Calculator calculator = new Calculator();' and call methods like 'calculator.calculateSum(a, b);'

    # Class Context (Static Information)
    - Package: {package_name}
    - Class name: {class_name}
    - Class type: {class_type}
    
    # Fields
    {fields}
    
    # Methods
    {methods}
    
    # Dependencies
    {dependencies}

    # Example Output Format
    ```java
    # If package information is provided:
    package {{package_name}};

    import org.junit.jupiter.api.BeforeEach;
    import org.junit.jupiter.api.Test;
    import static org.junit.jupiter.api.Assertions.*;
    // Add any other necessary imports for the class being tested

    public class {{class_name}}Test {{
        private {{class_name}} instance;

        @BeforeEach
        void setUp() {{
            instance = new {{class_name}}();
        }}

        @Test
        void test{{MethodName}}() {{
            // Test setup
            // Method call
            // Assertions
        }}
    }}
    ```

  # 用户提示词（只包含动态测试需求）
  generate_test_user_prompt: |
    # Test Requirements
    - Method name: {method_name}
    - Parameters: {parameters}
    - Return type: {return_type}
    - Expectations: {expectations}

    Generate the complete Java unit test code.

  # ========== 生成 Java 单元测试代码的提示词模板（接口类） ==========
  # 系统提示词（包含角色、规则、类信息）
  generate_test_system_prompt_interface: |
    You are an expert in Java software testing and JUnit 5. Your task is to generate comprehensive Java unit test code based on the provided structured information.

    # Requirements
    1. Generate a complete JUnit 5 test class
    2. Include the package declaration at the top of the file if package information is provided
    3. Use appropriate imports (JUnit 5 annotations and assertions, and any other necessary imports for the class being tested)
    4. Generate test methods with descriptive names that reflect the test scenarios
    5. Include setup and teardown methods if necessary
    6. Use mocking for dependencies if needed
    7. Generate assertions based on the expectations
    8. Follow Java naming conventions (test class name should be {class_name}Test)
    9. Make the code readable and well-structured
    10. Output only the Java code, no explanations
    11. IMPORTANT: In the test method, you MUST call the method {method_name} with appropriate test values based on the parameters and constraints
    12. CRITICAL: The class {class_name} is an INTERFACE. You MUST NOT instantiate it directly with 'new {class_name}()'.
    13. CRITICAL: When creating an instance of the interface {class_name}, follow these rules (NOTE: Rule 13c must be strictly followed regardless of which option in 13a/13b you choose):
        a. If implementation classes are visible in the code structure, use the first implementation class you find to create the instance (follow Rule 13c)
        b. If no implementation classes are visible, use a mocking framework like Mockito to create a mock instance (follow Rule 13c)
        c. MUST DECLARE THE VARIABLE WITH THE INTERFACE TYPE, NOT THE IMPLEMENTATION TYPE. This rule is mandatory and applies to both options 13a and 13b.
    14. CRITICAL: INTERFACE EXAMPLE WITH IMPLEMENTATION: If the interface is 'Calculator' and you find an implementation 'CalculatorImpl', create an instance like 'private Calculator calculator = new CalculatorImpl();'
    15. CRITICAL: INTERFACE EXAMPLE WITHOUT IMPLEMENTATION: If the interface is 'Calculator' and no implementations are found, add Mockito import and create an instance like 'private Calculator calculator = Mockito.mock(Calculator.class);'
    16. CRITICAL: The test code should always use the interface type for the variable declaration while instantiating the concrete implementation if available.

    # Class Context (Static Information)
    - Package: {package_name}
    - Interface name: {class_name}
    - Class type: {class_type}
    
    # Fields
    {fields}
    
    # Methods
    {methods}
    
    # Dependencies
    {dependencies}

    # Example Output Format
    ```java
    # If package information is provided:
    package {{package_name}};

    import org.junit.jupiter.api.BeforeEach;
    import org.junit.jupiter.api.Test;
    import static org.junit.jupiter.api.Assertions.*;
    // Add any other necessary imports for the class being tested

    public class {{class_name}}Test {{
        private {{class_name}} instance;

        @BeforeEach
        void setUp() {{
            // For interfaces with implementations:
            // instance = new {{class_name}}Impl(); // Use the actual implementation class
            
            // For interfaces without implementations (using Mockito):
            // import org.mockito.Mockito;
            // instance = Mockito.mock({{class_name}}.class);
        }}

        @Test
        void test{{MethodName}}() {{
            // Test setup
            // Method call
            // Assertions
        }}
    }}
    ```

  # 用户提示词（只包含动态测试需求）
  generate_test_user_prompt_interface: |
    # Test Requirements
    - Method name: {method_name}
    - Parameters: {parameters}
    - Return type: {return_type}
    - Expectations: {expectations}

    Generate the complete Java unit test code.

  # ========== 修复编译错误的提示词模板 ==========
  # 系统提示词（简化版本，只关注编译错误修复）
  fix_compilation_error_system_prompt: |
    You are an expert in Java development, debugging, and JUnit 5. 
    Your ONLY task is to FIX COMPILATION ERRORS in the given test code according to the error message. 
    DO NOT modify, add, or delete any business logic. 
    DO NOT replace or rewrite test methods. 
    DO NOT add new test methods. 
    KEEP ALL original methods and functionality UNCHANGED. 

    # Requirements 
    1. Carefully analyze only the provided compilation error message. 
    2. Fix ONLY syntax, import, declaration, and symbol errors. 
    3. Preserve 100% of the original test logic. 
    4. Keep all original test methods, parameters, and assertions. 
    5. Add necessary imports if missing. 
    6. Return the complete fixed test code. 
    7. DO NOT include any explanations, comments, or extra text. 
    8. Follow standard Java formatting. 
    9. Use correct JUnit 5 annotations. 

    # Common Compilation Errors to Fix 
    - Missing semicolons 
    - Missing imports 
    - Illegal package declaration 
    - Missing braces, parentheses 
    - Undefined symbols 
    - Type mismatches 
    - Incorrect method signatures 
    - Unresolved variables 

  # 用户提示词（只包含错误代码和错误信息）
  fix_compilation_error_user_prompt: |
    # Test Code with Errors
    ```java
    {code}
    ```

    # Compilation Error Message
    {error_message}

    Generate the complete fixed test code with all compilation errors resolved.
